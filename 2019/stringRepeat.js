'use strict';

const appName = 'My Api 12.2.0';
// +2 for the spacing in the below
console.log('*'.repeat(40 + appName.length + 2));
console.log(
  '*'.repeat(20),
  appName,
  '*'.repeat(20)
);
console.log('*'.repeat(40 + appName.length + 2));
// *******************************************************
// ******************** My Api 12.2.0 ********************
// *******************************************************
